
export type OptionAsideProps = {
    ReactIcon : React.ReactNode
    text?: String
}

export type TitleProps = {
    text: String
}

export type NoteProps = {
    id: number
    content : string
    createdAt: string
    title: string
    user_id : number
}
